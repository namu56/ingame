export enum Difficulty {
  DEFAULT = 'default',
  EASY = 'easy',
  NORMAL = 'normal',
  HARD = 'hard',
}

export enum Mode {
  MAIN = 'main',
  SUB = 'sub',
}

export enum Hidden {
  TRUE = 'true',
  FALSE = 'false',
}

export enum Status {
  COMPLETED = 'completed',
  FAIL = 'fail',
  ON_PROGRESS = 'onProgress',
}
