export enum Difficulty {
  Easy = 'EASY',
  Normal = 'NORMAL',
  Hard = 'HARD',
}

export enum Mode {
  Main = 'MAIN',
  Sub = 'SUB',
}

export enum isHidden {
  True = 'TRUE',
  False = 'FALSE',
}

export enum Status {
  Completed = 'COMPLETED',
  Fail = 'FAIL',
  onProgress = 'ON_PROGRESS',
}
