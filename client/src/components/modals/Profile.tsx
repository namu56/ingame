import styled from 'styled-components';

const Profile = () => {
  return (
    <ProfileStyle>
      <div></div>
    </ProfileStyle>
  );
};

const ProfileStyle = styled.div``;

export default Profile;
