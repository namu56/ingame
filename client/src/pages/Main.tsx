import styled from 'styled-components';
import Ranking from './Ranking';

const Main = () => {
  return (
    <MainStyle>
      <Ranking />
    </MainStyle>
  );
};

const MainStyle = styled.div``;

export default Main;
